<h1>Комментарий</h1>
<p>
  <strong>Пост:</strong> <%= @comment.post.title %>
</p>
<p>
  <strong>Пользователь:</strong> <%= @comment.user.name %>
</p>
<p>
  <strong>Содержимое:</strong> <%= @comment.content %>
</p>
<%= button_to 'Изменить', edit_comment_path(@comment), method: :get, form: { class: 'inline-button' } %>
<%= button_to 'Назад', comments_path, method: :get, form: { class: 'inline-button' } %>
