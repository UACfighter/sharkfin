<h1>Country: <%= @country.name %></h1>
<%= button_to 'Edit', edit_country_path(@country), method: :get, form: { class: 'inline-button' } %>
<%= button_to 'Back', countries_path, method: :get, form: { class: 'inline-button' } %>
