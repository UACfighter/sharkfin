<h1>Roles</h1>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @roles.each do |role| %>
      <tr>
        <td><%= role.name %></td>
        <td><%= button_to 'Show', role, method: :get, form: { class: 'inline-button' } %></td>
        <td><%= button_to 'Edit', edit_role_path(role), method: :get, form: { class: 'inline-button' } %></td>
        <td><%= button_to 'Destroy', role, method: :delete, data: { confirm: 'Are you sure?' }, form: { class: 'inline-button' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= button_to 'New Role', new_role_path, method: :get, form: { class: 'inline-button' } %>
